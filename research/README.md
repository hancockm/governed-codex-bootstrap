# Research intake

Place untrusted source material in `inbox/`, then use the intake command in the root README. The tool copies it to `records/` under a content hash and writes provenance metadata. Files in `records/` are immutable evidence. `schema.json` describes the metadata record; `example-record.json` is a non-authoritative example.
