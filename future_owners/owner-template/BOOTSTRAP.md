# Future-owner activation bootstrap

Status: inactive. Core must first accept this owner's bounded authority, files,
decisions, verification surfaces, model binding, and continuity ownership. Then
update the registry, complete [owner_profile.json](owner_profile.json), create
the continuity MOC, author the four mapped documents under [canonical/](canonical),
and add owner-specific packet checks before changing `active` to `true`.

The owner must read and reconcile its Core Thesis, Architecture, Spec, and
Implementation Roadmap during adoption. Missing, placeholder-only, or
unmapped owner canonical documents block activation.
